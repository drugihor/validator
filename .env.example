# Environment variables for Email Validator API
# Copy this file to .env and set your actual values

# API Key for authentication (generate a strong random key)
API_KEY=your_strong_api_key_here_replace_with_actual_key

# Optional: Flask environment settings
FLASK_ENV=development
FLASK_DEBUG=true